//
// This file is released under the terms of the NASA Open Source Agreement (NOSA)
// version 1.3 as detailed in the LICENSE file which accompanies this software.
//

{
	
	//===============================// 
	//==== Standard Parm Button  ====//
	xLocButton = new ParmButton( this, ui->xLocButton );
	yLocButton = new ParmButton( this, ui->yLocButton );
	zLocButton = new ParmButton( this, ui->zLocButton );

	xRotButton = new ParmButton( this, ui->xRotButton );
	yRotButton = new ParmButton( this, ui->yRotButton );
	zRotButton = new ParmButton( this, ui->zRotButton );

	originButton = new ParmButton( this, ui->rotOriginButton );

	numPntsButton = new ParmButton( this, ui->numPointsButton );
	numXsecsButton = new ParmButton( this, ui->numXsecsButton  );

	densityButton = new ParmButton( this, ui->denistyNameButton );
	shellMassAreaButton = new ParmButton( this, ui->shellMassAreaNameButton );



}

