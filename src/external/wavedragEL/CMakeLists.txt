CMAKE_MINIMUM_REQUIRED(VERSION 3.1)

SET(WAVEDRAGEL_INCLUDE_DIR ${CMAKE_CURRENT_SOURCE_DIR} CACHE PATH "Path to wavedragEL library")

INCLUDE_DIRECTORIES(${WAVEDRAGEL_INCLUDE_DIR}
    )

ADD_LIBRARY(wavedragEL
wavedragEL.c
wavedragEL.h
)