CMAKE_MINIMUM_REQUIRED( VERSION 3.1 )

SET( CLIPPER_INCLUDE_DIR ${CMAKE_CURRENT_SOURCE_DIR} CACHE PATH "Path to Clipper headers" )

INCLUDE_DIRECTORIES( ${CLIPPER_INCLUDE_DIR} )

ADD_LIBRARY( clipper
clipper.hpp
clipper.cpp )