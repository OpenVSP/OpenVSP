/*
 * The script optionally starts with a UTF8 encoded byte-order-mark.
 *
 * This script really does not do anything important.
 * It is just here to test that the generated bytecode
 * is correct.
 *
 * A comment in UTF8: ♥♣♠♦
 *
 */
#include "include.as"

float main()
{
	MyIncludedFunction();

	float ret = 1;
	float ret2 = 0;
	float ret3 = 1;
	int i2 = 0;
	for (int i = 0; i < 2; i++) {
		ret = ret * (ret+1);
		ret2 += float(i);
		ret3 /= 1.1f;
		i2++;
		i2++;
	}
	int8 a = 1, b = 2, c = 3, d = 4, e = 5, f = 6;
	a = b + c + d + e + f;
	
	// A string with 4 UTF8 encoded characters
	string poker = "♥♣♠♦";
	assert(poker.length() == 12);

	return ret + ret2 + ret3;
}
