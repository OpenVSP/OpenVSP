CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

INCLUDE_DIRECTORIES( ${LIBXML2_INCLUDE_DIR} ${UTIL_INCLUDE_DIR} )

SET(XMLVSP_INCLUDE_DIR ${CMAKE_CURRENT_SOURCE_DIR} CACHE PATH "Path to XML wrapper header.")

ADD_LIBRARY(xmlvsp
XmlUtil.h
XmlUtil.cpp

)
